# AI桌宠设计文档

## 项目进度概览

| 模块 | 状态 | 说明 |
|------|------|------|
| 核心架构 | ✅ 完成 | 状态管理器、Agent、Skill引擎已完成 |
| 记忆系统 | ✅ 完成 | 分级存储、召回机制、**嵌入向量检索**已完成 |
| 好感度/情绪 | ✅ 完成 | 多维状态系统已完成 |
| 角色卡 | ✅ 完成 | 配置加载、Live2D支持 |
| WebUI | 🔄 进行中 | 基础框架已完成，部分页面待完善 |
| 数据库 | ✅ 完成 | 符合设计文档Schema |
| 桌宠界面 | 🔄 进行中 | Live2D加载已完成，窗口管理待完善 |
| TTS/语音 | 🔄 进行中 | 客户端已搭建，TTS服务端独立部署，语音识别未完成 |
| 资料库管理 | ❌ 待完成 | 界面未实现 |
| 插件管理 | ❌ 待完成 | Skill安装/卸载API未完成 |
| 任务管理 | ❌ 待完成 | 增删改查API未实现 |
| 定时提醒 | 🔄 部分 | 数据库操作完成，触发执行未完成 |
| 日记功能 | ❌ 待完成 | 未实现 |
| 游戏化元素 | ⏸️ 暂缓 | 留待后续迭代 |
| 嵌入检索 | ✅ 完成 | 向量搜索+BM25+jointSearch联合检索 |
| 用户状态记忆 | ❌ 待实现 | 独立于对话记忆，记忆用户生活/工作状态，分级存储 |

---

## 1. 项目总览 ✅

本项目旨在开发一款具备高度个性化和成长性的AI桌宠。桌宠通过多模态感知与用户交互，拥有独立的情感和记忆系统，并能根据与用户的互动逐步成长和变化。项目核心是一个基于多模态Agent的协作集群，所有AI推理均通过联网API完成，确保在无独立显卡的电脑上也能流畅运行。

### 1.1 伦理与设计原则 ✅
- **始终可选**：所有自动化功能（如主动对话、环境感知）都可由用户关闭。
- **透明度**：让用户知道桌宠“看到”了什么（例如，显示屏幕分析结果）。
- **可控性**：用户可以审查和删除任何关于自己的记忆。
- **错误处理**：API调用失败时，在对话框报错提示用户查看日志，日志记录详细错误信息。
- **隐私保护**：除必要的LLM/TTS请求通信外，不向其他服务器上传任何数据，不收集用户数据。
- **渐进亲密**：随着好感度提升，逐步开放更高频的主动交互和更多功能。
- **轻量化**：AI推理全部使用联网API，本地仅运行核心协调器和界面，确保低硬件要求。

---

## 2. 核心系统架构 ✅

系统采用高内聚、低耦合的模块化设计，核心是一个中央协调器（状态管理器）和多个功能Agent。

### 2.1 核心组件 ✅
- **状态管理器（中央协调器）**：✅ 已实现
    - 维护全局状态：好感度（多维）、情绪状态。
    - 负责心跳任务调度。
    - 协调各Agent之间的通信和数据流。
- **分析Agent（接入多模态模型）**：✅ 已实现
    - **核心职责**：上下文管理、复杂任务分解、工具调用。
    - 工作流程：分析用户输入 -> LLM根据上下文自行决定是否调用工具（可能0个，可能多个）-> 调用对话润色Skill -> 生成回复。
    - **工具调用决策**：由LLM根据上下文自行判断是否需要调用搜索、屏幕分析等工具。
    - **带Loop的检索**：✅ 已实现（最多6轮迭代）
    - **工具调用**：✅ Skill引擎已实现，支持搜索、图像分析等skill
    - **模型支持**：✅ 多模态模型配置已完成
    - **回复输出**：✅ SSE流式输出，支持text/action事件类型

### 2.2 心跳任务 ✅
系统每隔1分钟执行一次心跳任务，用于处理周期性工作：
- **定时任务检查**：✅ 数据库操作完成，触发执行待完善
- **环境感知与主动交互**：✅ 框架完成（随机记忆召回）
- **记忆系统维护**：✅ checkAndSummarizeAll已完成
- **状态更新**：✅ tick()情绪回归已实现

### 2.3 Skill系统 ✅

Skill是教AI按固定流程做事的操作说明书，本质是Markdown文件（SKILL.md），可像函数一样被调用。

**核心定位**：✅ 已实现
- **Skills**：知识复用（经验、最佳实践、工作流程），基于Markdown文件，渐进式加载，Token效率高。

**Skill目录结构**：✅ 已定义
```
skills/
└── <skill-name>/           # 技能文件夹（kebab-case小写）
    ├── SKILL.md            # 唯一必需文件（核心指令 + YAML frontmatter）
    ├── scripts/            # 可选：可执行代码
    ├── references/         # 可选：文档资料
    └── assets/             # 可选：模板、资源
```

**SKILL.md格式**：✅ 已定义

**加载机制**：✅ 已实现

---

## 3. 感知与交互功能

### 3.1 环境感知（输入）🔄 进行中
- **实时屏幕分析**：⚠️ Skill存在但未集成到心跳任务
- **时间上下文**：✅ 已实现（通过系统时间）
- **地理位置/天气**：❌ 未实现（API未接入）
- **电脑文件读取**：⚠️ 仅支持工作区目录读写

### 3.2 主动交互（输出）🔄 部分
用户可调节主动交互频率，上限由**角色卡**决定——由角色设计者设定时间间隔（支持随机化）。包括：
- **环境触发**：⚠️ 框架完成，未集成
- **定时任务**：🔄 数据库完成，触发执行待完善
- **随机记忆召回**：✅ 已实现（checkProactiveInteraction）
- **日记功能**：❌ 未实现

---

## 4. 记忆系统设计 ✅

记忆系统采用分级存储和智能召回机制，所有数据存储于SQLite。

### 4.1 分级存储结构 ✅
1.  **短期记忆**：✅ 已实现
    - **内容**：当前话题的多轮对话总结（非原始对话）。
    - **机制**：✅ LLM自动识别话题切换
2.  **长期记忆**：✅ 已实现
    - **粒度**：✅ year/season/month/week/day/topic七级存储
    - **存储方式**：✅ 原始对话+记忆表分离
    - **总结机制**：✅ checkAndSummarizeAll定时总结
    - **触发条件**：✅ 心跳任务中检查
    - **属性**：✅ 包含关键词、时间标签
    - **嵌入向量**：✅ 自动生成并存储，支持向量检索
3.  **用户画像**：⚠️ JSON文件存在，但无LLM更新机制
    - **内容**：✅ 基本信息结构已定义
    - **格式**：✅ JSON文件
    - **存储**：✅ data/user_profile.json
    - **更新**：❌ 每日更新机制未实现
4.  **资料库**：✅ 存储层完成
    - **内容**：✅ knowledge_base表
    - **格式**：✅ 与长期记忆格式一致
    - **检索**：✅ search方法已实现（含嵌入向量检索）

### 4.2 记忆召回逻辑 ✅
- **关键词生成**：✅ generateKeywords已实现
- **分层检索**：✅ getCoarserMemories已实现
- **主动召回**：✅ recallRandomMemory已实现
- **向量检索**：✅ EmbeddingManager + vectorSearch实现
- **BM25检索**：✅ bm25Search实现
- **联合检索**：✅ jointSearch实现（向量+关键词融合）
- **重排序**：✅ reranker实现

---

## 5. 个性化成长系统 ✅

### 5.1 好感度系统（多维）✅

好感度由多个独立维度构成，各维度互不影响，在使用时各维度的提示词**平行插入上下文**。

- **维度**：✅ 由角色设计者在character.json中定义（信赖度、亲密度、占有欲）
- **数值范围**：✅ 角色卡配置决定
- **使用方式**：✅ stateManager.getAffinityStagePrompts()已实现
- **主动交互频率**：🔄 框架完成，频率控制待完善
- **配置**：✅ affinityStages在character.json中完整定义
- **状态更新Prompt**：✅ 已集成到角色卡配置

### 5.2 情绪状态系统 ✅

情绪状态是短期动态状态，随时间向平静值回归。

- **维度**：✅ 角色卡定义（悲-喜、愤怒-平静、焦虑-放松）
- **数值范围**：✅ 角色卡配置决定
- **对话影响**：✅ getEmotionDescription()已实现
- **极端情绪时的行为**：✅ character.json中定义

### 5.3 状态变化规则 ✅
- **异步更新**：✅ updateStatesAsync已实现
- **更新机制**：✅ 每轮对话后由LLM判断
- **变化幅度**：✅ 角色卡定义范围
- **心跳调整**：✅ tick()已实现情绪回归

### 5.4 可视化记忆管理页面 🔄 部分
提供WebUI页面用于查看和管理记忆：
- **长期记忆**：✅ DatabaseView中可查看
- **用户画像**：⚠️ 有存储，但无独立管理界面
- **隐藏数据**：⚠️ 好感度/情绪状态未在界面显示

---

## 6. 游戏化元素 ⏸️

> **暂不实现**，留待后续迭代。

- **成就系统**：❌ 未实现
- **迷你游戏**：❌ 未实现
- **挂机收集系统**：❌ 未实现
- **商店系统**：❌ 未实现

---

## 7. 角色卡系统 ✅

角色卡是一个可导入导出的预设文件，是角色灵魂的载体。

### 7.1 角色卡内容 ✅
- **Live2D模型**：✅ character-cards/satori/live2d/ 已配置
- **LLM配置**：✅ 通过.env配置
- **语音合成（TTS）**：✅ 角色卡包含TTS配置
- **角色身份信息**：✅ character.json完整定义
- **角色语言风格**：✅ dialogueRequirements已定义
- **好感度系统**：✅ affinityStages已定义（信赖度、亲密度、占有欲）
- **情绪状态系统**：✅ emotionStages已定义（悲-喜、愤怒-平静、焦虑-放松）
- **状态更新Prompt**：✅ 已集成
- **主动交互频率**：🔄 待完善
- **角色世界观资料**：✅ character-cards/satori/knowledge/
- **备注信息**：✅ character.json中包含

### 7.2 角色卡格式 🔄 部分
- **格式**：⚠️ 定义为ZIP包，实际为文件夹直接加载
- **内容**：✅ 配置JSON + Live2D文件 + TTS配置
- **导入导出**：❌ 未实现

---

## 8. 桌宠界面（前端）🔄 进行中

一个无边框、悬浮置顶的窗口，仅显示Live2D模型和少量UI。

**窗口默认行为**：🔄 进行中
- 竖长透明窗口，位于桌面右下角。

### 8.1 界面元素 🔄 部分
- **Live2D模型**：✅ live2d-widget已实现加载
- **对话框**：❌ 未集成到桌宠窗口
- **字幕（无框）**：❌ 未实现
- **首次启动引导**：❌ 未实现

### 8.2 右键菜单 ❌ 未实现
- **打开管理页面**：⚠️ 需手动打开WebUI
- **互动**：❌ 未实现
- **变装**：❌ 未实现
- **隐藏**：❌ 未实现
- **设置互动频率**：❌ 未实现
- **退出**：❌ 未实现

### 8.3 语音播放 🔄 部分
- 角色回复文字时，同步播放TTS语音。✅ TTS客户端已实现，与WebUI协同

### 8.4 其他要求 ✅
- 动态加载当前角色卡对应的Live2D模型等配置，不允许使用默认硬编码。✅ 已实现
- 遵从let it crash原则，不允许使用缺省值来规避错误，任何配置错误都必须在界面上有明显提示，指导用户修正。✅ API返回明确错误信息
- 对齐后端已有的API设计，确保前后端协作顺畅。✅ API路由已定义

---

## 9. WebUI管理界面（技术栈：Vue3 + TypeScript）🔄 进行中

所有数据操作均通过WebUI封装好的按钮和输入框等进行，不直接操作数据库或JSON文件，确保操作门槛低。

### 9.0 URL路由设计 ✅ 已实现

| 路径 | 页面 | 状态 |
|------|------|------|
| `/` | 首页（欢迎页 + 快速入口） | ✅ 已实现 |
| `/characters` | 角色卡管理列表 | ❌ 未实现 |
| `/characters/new` | 新建角色 | ❌ 未实现 |
| `/characters/:id` | 角色详情/编辑 | ❌ 未实现 |
| `/memories` | 记忆管理 | ❌ 未实现 |
| `/knowledge` | 资料库管理 | ❌ 未实现 |
| `/settings` | 配置管理 | ❌ 未实现 |
| `/plugins` | 插件管理（Skill） | ❌ 未实现 |
| `/tasks` | 任务管理 | ❌ 未实现 |
| `/status` | 状态面板 | ✅ 已实现 |
| `/chat` | 对话测试页面 | ✅ 已实现 |

### 9.1 功能模块 🔄 部分
0.  **欢迎页**：✅ HomeView已实现
1.  **角色卡管理**：❌ 未实现
2.  **记忆管理**：⚠️ DatabaseView可查看，无独立管理页面
3.  **配置管理**：❌ 未实现（.env配置）
4.  **资料库管理**：❌ 未实现
5.  **插件管理**：❌ 未实现
6.  **任务管理**：❌ 未实现
7.  **状态面板**：✅ StatusView已实现
8.  **对话测试页面**：✅ ChatView已实现

---

## 10. 数据与配置存储

- **数据库**：SQLite。
    - 存储内容：长期记忆、资料库、定时任务、系统状态等。**用户画像存放在JSON文件中**。
    - 提供统一接口供Agent调用和WebUI管理。
- **配置文件**：使用`.env`文件存储敏感配置（如API密钥）。
    - **安全要求**：敏感信息（API密钥等）优先使用环境变量存储，不明文暴露在配置文件中。
    - **配置方式**：通过WebUI配置界面管理，不手动编辑文件。初次启动时若没有则自动生成模板。
    - 存储内容：全局配置，如API密钥、隐私设置等。
    - 提供接口供Agent调用和WebUI管理。
- **目录结构**（均位于项目目录下）：
    - `data/`：用户数据目录（数据库文件、用户画像JSON、工作区文件夹、日志文件夹等）
    - `skills/`：技能目录
    - `.env`：配置文件（自动生成）

### 10.1 数据库Schema ✅

#### 表1: dialogues（原始对话表）✅
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| turn_index | INTEGER | 对话轮次序号 |
| user_content | TEXT | 用户输入 |
| ai_content | TEXT | AI回复 |
| created_at | DATETIME | 创建时间 |

#### 表2: memories（记忆表）✅
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| granularity | TEXT | 粒度：year/season/month/week/day/topic |
| content | TEXT | 记忆内容（总结） |
| keywords | TEXT | 关键词（JSON数组） |
| period_start | DATETIME | 记忆覆盖时间段起始 |
| period_end | DATETIME | 记忆覆盖时间段结束 |
| created_at | DATETIME | 创建时间 |

#### 表3: tasks（定时任务表）✅
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| name | TEXT | 任务名称 |
| cron | TEXT | Cron表达式 |
| action_type | TEXT | 动作类型：reminder/notification |
| params | TEXT | 动作参数（JSON） |
| enabled | INTEGER | 是否启用（0/1） |
| last_run | DATETIME | 上次执行时间 |
| next_run | DATETIME | 下次执行时间 |
| created_at | DATETIME | 创建时间 |

#### 表4: system_state（系统状态表）✅
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 主键（状态项名称，如"affinity"/"emotion"） |
| character_id | TEXT | 角色卡ID（与id组成联合主键） |
| value | TEXT | 状态值（JSON格式） |
| updated_at | DATETIME | 更新时间 |

#### 表5: knowledge_base（资料库表）✅
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| category | TEXT | 分类 |
| content | TEXT | 内容 |
| keywords | TEXT | 关键词（JSON数组） |
| source | TEXT | 来源：character_card/user |
| created_at | DATETIME | 创建时间 |

#### 表6: logs（日志表）✅
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| level | TEXT | 日志级别：info/warn/error/debug |
| category | TEXT | 日志分类：screen_analysis/api_call/heartbeat/task等 |
| content | TEXT | 日志内容 |
| created_at | DATETIME | 创建时间 |

**日志清理机制**：✅ 已实现
- 日志暂时存储在数据库中。
- 当日志达到500条时，触发日志输出：所有累积日志以txt文件形式输出到日志文件夹，然后已输出的日志从数据库中清除。
- txt日志文件按日期命名。

---

## 11. 用户状态记忆系统 ❌ 待实现

用户状态记忆是一个独立于对话记忆的新系统，专门记忆用户的**生活和工作状态**。它从对话中提取用户的状态信息（如工作项目、生活事件、健康状态等），采用与记忆系统相同的分级存储和汇总机制。

### 11.1 核心定位

- **目的**：让AI了解用户的真实生活状态，而非仅仅记住对话内容
- **与记忆系统的区别**：
  - 记忆系统：记住"我们聊了什么"（对话内容总结）
  - 用户状态记忆：记住"用户最近在做什么、状态如何"（用户生活推断）
- **数据来源**：LLM从每轮对话中提取用户状态信息
- **使用场景**：AI在主动交互时可以基于用户当前状态发起更有意义的对话（例如："你上周说项目deadline快到了，现在怎么样了？"）

### 11.2 分级存储结构

与记忆系统相同的金字塔模型，但内容聚焦用户状态：

1. **状态快照（state）** — 最细粒度
   - **内容**：单次对话中提取的用户状态片段
   - **示例**："用户提到最近在赶项目deadline，工作压力大"
   - **触发**：每轮对话后LLM判断是否有新的用户状态信息

2. **日总结（day）** — 当日状态汇总
   - **阈值**：24小时
   - **子粒度**：state
   - **内容**：当天用户整体状态概览

3. **周总结（week）** — 周状态汇总
   - **阈值**：7天
   - **子粒度**：day

4. **月总结（month）** — 月状态汇总
   - **阈值**：4周
   - **子粒度**：week

5. **年总结（year）** — 年度状态汇总
   - **阈值**：365天
   - **子粒度**：month