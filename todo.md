# AI桌宠设计文档

## 1. 项目总览

本项目旨在开发一款具备高度个性化和成长性的AI桌宠。桌宠通过多模态感知与用户交互，拥有独立的情感和记忆系统，并能根据与用户的互动逐步成长和变化。项目核心是一个基于多模态Agent的协作集群，所有AI推理均通过联网API完成，确保在无独立显卡的电脑上也能流畅运行。

### 1.1 伦理与设计原则
- **始终可选**：所有自动化功能（如主动对话、环境感知）都可由用户关闭。
- **透明度**：让用户知道桌宠“看到”了什么（例如，显示屏幕分析结果）。
- **可控性**：用户可以审查和删除任何关于自己的记忆。
- **错误处理**：API调用失败时，在对话框报错提示用户查看日志，日志记录详细错误信息。
- **隐私保护**：除必要的LLM/TTS请求通信外，不向其他服务器上传任何数据，不收集用户数据。
- **渐进亲密**：随着好感度提升，逐步开放更高频的主动交互和更多功能。
- **轻量化**：AI推理全部使用联网API，本地仅运行核心协调器和界面，确保低硬件要求。

---

## 2. 核心系统架构

系统采用高内聚、低耦合的模块化设计，核心是一个中央协调器（状态管理器）和多个功能Agent。

### 2.1 核心组件
- **状态管理器（中央协调器）**：
    - 维护全局状态：好感度（多维）、情绪状态。
    - 负责心跳任务调度。
    - 协调各Agent之间的通信和数据流。
- **分析Agent（接入多模态模型）**：
    - **核心职责**：上下文管理、复杂任务分解、工具调用。
    - 工作流程：分析用户输入 -> LLM根据上下文自行决定是否调用工具（可能0个，可能多个）-> 调用对话润色Skill -> 生成回复。
    - **工具调用决策**：由LLM根据上下文自行判断是否需要调用搜索、屏幕分析等工具。简单对话（如问候）可能完全不调用工具，直接进入润色流程；复杂对话会触发多轮检索。
    - **带Loop的检索**：可进行多轮检索和工具调用，根据中间结果决定下一步行动。
    - **工具调用**：工具采用`skill`形式，方便扩展。
        - 内置基础skill：搜索（长期记忆/资料库/网络检索）、定时任务、屏幕分析、对话润色（语言风格润色+动作表情生成）。
        - 其他skill由用户自行安装扩展。
    - **模型支持**：多模态模型由用户在配置中自定义，角色卡中可建议但以用户配置为准。支持国内模型（通义千问、智谱GLM等）。
    - **回复输出**：采用SSE流式输出，支持多事件类型：
        - `text`事件：文字内容，用于TTS播放和显示
        - `action`事件：Live2D动作指令，JSON格式 `{"type": "动作类型", "params": {...}}`
      前端实时解析事件类型，text事件触发TTS和显示，action事件触发Live2D动作。

### 2.2 心跳任务
系统每隔1分钟执行一次心跳任务，用于处理周期性工作：
- **定时任务检查**：检查并执行到期的定时任务（如提醒）。
- **环境感知与主动交互**：根据用户设定的频率，触发屏幕分析skill（由LLM自行决定是否分析屏幕内容）或随机召回记忆，主动发起对话。频率上限受角色卡设定的好感度维度数值影响。
- **记忆系统维护**：触发记忆总结（如日总结、周总结）和用户画像更新。
- **状态更新**：将情绪状态向”平静”值调整。

### 2.3 Skill系统

Skill是教AI按固定流程做事的操作说明书，本质是Markdown文件（SKILL.md），可像函数一样被调用。

**核心定位**：
- **Skills**：知识复用（经验、最佳实践、工作流程），基于Markdown文件，渐进式加载，Token效率高。

**Skill目录结构**：
```
skills/
└── <skill-name>/           # 技能文件夹（kebab-case小写）
    ├── SKILL.md            # 唯一必需文件（核心指令 + YAML frontmatter）
    ├── scripts/            # 可选：可执行代码
    ├── references/         # 可选：文档资料
    └── assets/             # 可选：模板、资源
```

**SKILL.md格式**：
```yaml
---
name: skill-name            # 必填：技能名（小写字母、数字、-）
description: >-              # 必填：功能与使用场景说明
trigger_keywords:           # 推荐：触发关键词列表
  - 关键词1
  - 关键词2
version: “1.0”               # 可选
author: xxx                 # 可选
---
# 正文：Markdown格式指令
```

**加载机制**：
- 启动时AI仅加载技能名称和描述（基本识别信息）。
- 任务匹配时，AI自动读取完整SKILL.md指令，按需加载子文件夹内容。
- 支持本地文件夹安装、远程URL安装。

---

## 3. 感知与交互功能

### 3.1 环境感知（输入）
- **实时屏幕分析**：通过**屏幕分析Skill**实现。**触发时机**：LLM在每次回复前先判断"是否需要分析屏幕"，然后决定是否调用。截图分辨率可配置（默认压缩），存储位置可配置。分析结果存入**日志**以满足透明度要求，不会上传到其他服务器。
- **时间上下文**：工作时间、休息时间、节假日、最近互动频率、上一次对话时间。
- **地理位置/天气**：通过联网API获取。
- **电脑文件读取**：桌宠拥有自己的**工作区**（`data/workspace/`），可在其中自由读写。**工作区外的文件**需由用户主动提供（输入路径、拖拽文件到桌宠上等）才能读写。默认支持读取纯文本（.txt/.md/.json等）和图片（.png/.jpg/.gif等）格式，其他格式需安装对应skill。

### 3.2 主动交互（输出）
用户可调节主动交互频率，上限由**角色卡**决定——由角色设计者设定时间间隔（支持随机化）。包括：
- **环境触发**：根据屏幕内容主动评论（如“这部电影看起来很有趣”）。
- **定时任务**：执行用户设定的任务（如每天早上问候、提醒喝水）。
- **随机记忆召回**：突然提起过去某次对话的内容（如“你还记得上周末我们聊的那件事吗？”）。
- **日记功能**：
    - 每天根据当日全部对话，生成一篇带有角色性格、关系色彩的日记。
    - 日记保存路径可配置（默认保存到桌面），用户可在设置中更改。
    - 日记内容同时存入长期记忆系统（记忆粒度为话题，存为对应那一天的最后一条记忆）。

---

## 4. 记忆系统设计

记忆系统采用分级存储和智能召回机制，所有数据存储于SQLite。

### 4.1 分级存储结构
1.  **短期记忆**：
    - **内容**：当前话题的多轮对话总结（非原始对话）。
    - **机制**：每轮对话结束后，由LLM对本次对话进行总结，更新短期记忆。话题切换由**LLM自动识别**——当用户发新话题或当前话题已充分讨论后认为需要切换时，当前短期记忆被封存为”话题”粒度的记忆存入长期记忆，并清空重置。若用户中途插入B话题再切回A，算作两个独立话题。
2.  **长期记忆**：
    - **粒度**：按”年 → 季 → 月 → 周 → 日 → 话题 → 单轮对话”七级存储。
    - **存储方式**：
        - **原始对话表**：存储所有单轮对话（用户一句 + AI一句为一轮），按时间顺序。
        - **记忆表**：存储话题及以上粒度的总结（以`粒度`字段区分），高级概括低级主要内容而省略细节。
    - **总结机制**：每24小时总结一次为”日”记忆；同理，7天总结为”周”，以此类推。原始对话内容保留。
    - **触发条件**：时间到了自动触发总结，在心跳任务中检查是否需要触发。
    - **属性**：每条记忆包含关键词、时间标签。
3.  **用户画像**：
    - **内容**：用户的基本信息、兴趣爱好、性格、习惯、对话偏好等。
    - **格式**：JSON文件，每条信息包含`id`、`内容`、`重要性`等属性。
    - **存储**：存放于用户数据目录（JSON文件），**不是SQLite**。
    - **更新**：用户可配置最大存储数量，存满时优先删除重要性低的记录。每日根据当日对话总结，异步调用LLM对画像进行更新（新增、修改或删除）。
4.  **资料库**：
    - **内容**：角色卡附带的世界观资料、用户自行添加的结构化信息。
    - **格式**：与长期记忆格式一致（无时间标签），可共同检索。

### 4.2 记忆召回逻辑
- **关键词生成**：由**LLM自动生成**，用户可在记忆管理页面手动编辑。每条记忆的关键词包括直接关键词（高权重）和联想关键词（低权重），并可指定时间范围。
- **分层检索**：
    - 首先根据关键词和时间范围召回最粗粒度的记忆。
    - 如果最粗粒度记忆仅一条，则自动细化召回下一粒度的记忆（例如，查询某月返回月总结及该月所有日总结）。
    - 如果粗粒度记忆内容提到新关键词但无具体内容，则触发二次细化检索。
- **主动召回**：
    - 在用户无明确关键词时，可进行模糊召回。
    - 当用户提及过往内容或表现出相关情绪时，主动召回相关话题记忆。

---

## 5. 个性化成长系统

### 5.1 好感度系统（多维）

好感度由多个独立维度构成，各维度互不影响，在使用时各维度的提示词**平行插入上下文**。

- **维度**：由角色设计者自由定义维度数量和名称（如"信赖度"、"占有欲"、"亲密度"等）。
- **数值范围**：各维度的数值范围由**角色卡决定**，连续值。
- **使用方式**：每个维度对应一段提示词，使用时各维度提示词平行插入上下文（不互相覆盖或叠加）。
- **主动交互频率**：由角色卡设定，可根据各维度数值动态调整。
- **配置**：角色设计者在角色卡中定义各维度的名称、数值范围、对应的提示词模板。
- **状态更新Prompt**：作为角色卡的可选属性，项目自带默认值，角色卡中配置则优先使用角色卡中的。

### 5.2 情绪状态系统

情绪状态是短期动态状态，随时间向平静值回归。

- **维度**（多维，互相独立，无制约关系）例如：
    - **喜 - 悲**：喜悦、愉快、平静、失落、悲伤。
    - **平静 - 愤怒**：平静、轻微不满、不满、愤怒、暴怒。
    - **放松 - 焦虑**：放松、轻微紧张、紧张、焦虑、极度焦虑。
- **数值范围**：各维度的数值范围由**角色卡决定**，多维度互相独立（例：愤怒时喜悦仍可能很高）。向平静值回归的速度由角色卡决定，用户可在WebUI中调节。
- **对话影响**：
    - 在system prompt中插入情绪状态文字描述。
    - 在回复"内容要点"中体现情绪。
    - 在润色Skill的提示词中插入情绪状态文字描述。
    - 极端情绪时的行为由角色卡作者在提示词中定义。

### 5.3 状态变化规则
- **异步更新**：状态变化不立即反应在对话中，而是在心跳任务或对话结束后进行，保证性能。
- **更新机制**：每轮对话结束后（**一轮 = 用户一句 + 桌宠一句**），后台调用**专用状态更新Prompt**，综合用户消息、AI回复、当前状态等，计算出新的好感度和情绪状态变化值。
- **变化幅度**：每次数值变化有上限和下限（由角色卡决定），LLM直接输出JSON格式的数值。
- **心跳调整**：心跳任务中的情绪调整速率很小，随时间平滑过渡，不会与对话状态更新冲突。

### 5.4 可视化记忆管理页面
提供一个WebUI页面，用于查看和管理记忆。
- **长期记忆**：按年/季/月/周/日/话题等粒度展示，支持时间范围或关键词搜索，可编辑和删除。
- **用户画像**：按重要性排序展示信息列表，支持编辑、删除。
- **隐藏数据**：好感度、情绪状态等数值在管理界面默认隐藏。

---

## 6. 游戏化元素

> **暂不实现**，留待后续迭代。

- **成就系统**：达成特定条件解锁成就。
- **迷你游戏**：内置简单小游戏，供用户娱乐。
- **挂机收集系统**：用户离开时，桌宠可自动进行”探索”或”工作”，收集游戏内资源。
- **商店系统**：使用挂机收集的资源或成就点，为桌宠购买新服装、新动作或特殊功能。

---

## 7. 角色卡系统

角色卡是一个可导入导出的预设文件，是角色灵魂的载体。

### 7.1 角色卡内容
- **Live2D模型**：可包含多个模型文件（对应不同皮肤/服装）。
- **LLM配置**：建议使用的模型（具体以用户配置为准）。
- **语音合成（TTS）**：建议使用的语音模型，可附带克隆用的音源文件。
- **角色身份信息**：姓名、年龄、性别、性格、身世等。
- **角色语言风格**：口头禅、语气、回复特点等。
- **好感度系统**：多维好感度的维度定义（数量、名称、数值范围）及各维度对应的提示词模板。
- **情绪状态系统**：三维情绪的数值范围和回归速度设定。
- **状态更新Prompt**：可选，自定义状态更新逻辑（项目有默认值）。
- **主动交互频率**：由角色卡设定的好感度维度数值动态影响的时间间隔（支持随机化）。
- **角色世界观资料**：背景故事、世界设定等，作为资料库的一部分。
- **备注信息**：其他设计者想要添加的备注或说明。

### 7.2 角色卡格式
- **格式**：ZIP压缩包（打包为一个文件夹），自动打包/解压工具。
- **内容**：配置JSON + Live2D文件 + 克隆用参考音频 + 其他资源。
- **导入导出**：支持从文件导入和导出为ZIP包。

---

## 8. 桌宠界面（前端）

一个无边框、悬浮置顶的窗口，仅显示Live2D模型和少量UI。

**技术选型**：待定。

**窗口默认行为**：
- 竖长透明窗口，位于桌面右下角。

### 8.1 界面元素
- **Live2D模型**：主体显示区域。可拖动和调整大小。并且记忆上次的位置和大小。
- **对话框**：用户输入文本的输入框。
- **字幕（无框）**：显示角色的回复内容。
- **首次启动引导**：桌宠对话框输出教程，引导用户打开WebUI进行配置（不弹窗）。

### 8.2 右键菜单
- **打开管理页面**：打开WebUI管理界面（两种方式：桌宠右键 → "打开管理页面"；或浏览器输入对应URL）。
- **互动**：预设互动选项（如摸头、拥抱），触发纯动作（无对话）。
- **变装**：切换不同皮肤/服装。
- **隐藏**：暂时隐藏桌宠，可设定自动出现时间。
- **设置互动频率**：设置主动交互频率，上限受好感度限制。
- **退出**：关闭桌宠。

### 8.3 语音播放
- 角色回复文字时，同步播放TTS语音。对话体验与webui中的效果一致。

### 8.4 其他要求
- 动态加载当前角色卡对应的Live2D模型等配置，不允许使用默认硬编码。
- 遵从let it crash原则，不允许使用缺省值来规避错误，任何配置错误都必须在界面上有明显提示，指导用户修正。
- 对齐后端已有的API设计，确保前后端协作顺畅。

---

## 9. WebUI管理界面（技术栈：Vue3 + TypeScript）

所有数据操作均通过WebUI封装好的按钮和输入框等进行，不直接操作数据库或JSON文件，确保操作门槛低。

### 9.0 URL路由设计

| 路径 | 页面 |
|------|------|
| `/` | 首页（欢迎页 + 快速入口） |
| `/characters` | 角色卡管理列表 |
| `/characters/new` | 新建角色 |
| `/characters/:id` | 角色详情/编辑 |
| `/memories` | 记忆管理（长期记忆 + 用户画像） |
| `/knowledge` | 资料库管理 |
| `/settings` | 配置管理（API密钥、隐私设置等） |
| `/plugins` | 插件管理（Skill） |
| `/tasks` | 任务管理 |
| `/status` | 状态面板（日志、心跳状态、Token计数等） |
| `/chat` | 对话测试页面 |

### 9.1 功能模块
0.  **欢迎页**：提供简介和简单教程；
1.  **角色卡管理**
    - **主界面**：显示角色列表，可以选择加载某一张角色卡。
2.  **记忆管理**
    - **长期记忆查看**：按粒度层级、时间范围、关键词检索，支持编辑/删除。
    - **用户画像管理**：按重要性排序，支持编辑/删除画像条目。
3.  **配置管理**
    - **API密钥管理**：管理LLM、天气、TTS等服务的BaseURL、Key、Model，支持连接测试。
    - **隐私设置**：开关控制是否允许访问文件、联网等，并提供说明。
4.  **资料库管理**
    - 结构化资料的增删改查，支持导入导出（JSON/CSV）。
5.  **插件管理**
    - **Skill管理**：查看、安装、卸载本地skill；支持从URL安装。
6.  **任务管理**
    - 查看、创建、编辑、删除定时任务。
7.  **状态面板**
    - 心跳任务执行状态（是否正常运行、上次心跳时间）。
    - API调用Token计数（累计Token使用量）。
    - 近期对话的全部上下文（包括后台分析过程）。
    - 系统日志查看（按分类筛选：screen_analysis/api_call/heartbeat/task/error等）。
8.  **对话测试页面**
    - 输入框测试对话输入，显示AI回复和触发的工具调用（如屏幕分析结果、检索到的记忆等），用于调试和验证系统功能。

---

## 10. 数据与配置存储

- **数据库**：SQLite。
    - 存储内容：长期记忆、资料库、定时任务、系统状态等。**用户画像存放在JSON文件中**。
    - 提供统一接口供Agent调用和WebUI管理。
- **配置文件**：使用`.env`文件存储敏感配置（如API密钥）。
    - **安全要求**：敏感信息（API密钥等）优先使用环境变量存储，不明文暴露在配置文件中。
    - **配置方式**：通过WebUI配置界面管理，不手动编辑文件。初次启动时若没有则自动生成模板。
    - 存储内容：全局配置，如API密钥、隐私设置等。
    - 提供接口供Agent调用和WebUI管理。
- **目录结构**（均位于项目目录下）：
    - `data/`：用户数据目录（数据库文件、用户画像JSON、工作区文件夹、日志文件夹等）
    - `skills/`：技能目录
    - `.env`：配置文件（自动生成）

### 10.1 数据库Schema

#### 表1: dialogues（原始对话表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| turn_index | INTEGER | 对话轮次序号 |
| user_content | TEXT | 用户输入 |
| ai_content | TEXT | AI回复 |
| created_at | DATETIME | 创建时间 |

#### 表2: memories（记忆表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| granularity | TEXT | 粒度：year/season/month/week/day/topic |
| content | TEXT | 记忆内容（总结） |
| keywords | TEXT | 关键词（JSON数组） |
| period_start | DATETIME | 记忆覆盖时间段起始 |
| period_end | DATETIME | 记忆覆盖时间段结束 |
| created_at | DATETIME | 创建时间 |

#### 表3: tasks（定时任务表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| name | TEXT | 任务名称 |
| cron | TEXT | Cron表达式 |
| action_type | TEXT | 动作类型：reminder/notification |
| params | TEXT | 动作参数（JSON） |
| enabled | INTEGER | 是否启用（0/1） |
| last_run | DATETIME | 上次执行时间 |
| next_run | DATETIME | 下次执行时间 |
| created_at | DATETIME | 创建时间 |

#### 表4: system_state（系统状态表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 主键（状态项名称，如"affinity"/"emotion"） |
| character_id | TEXT | 角色卡ID（与id组成联合主键） |
| value | TEXT | 状态值（JSON格式） |
| updated_at | DATETIME | 更新时间 |

#### 表5: knowledge_base（资料库表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| category | TEXT | 分类 |
| content | TEXT | 内容 |
| keywords | TEXT | 关键词（JSON数组） |
| source | TEXT | 来源：character_card/user |
| created_at | DATETIME | 创建时间 |

#### 表6: logs（日志表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| level | TEXT | 日志级别：info/warn/error/debug |
| category | TEXT | 日志分类：screen_analysis/api_call/heartbeat/task等 |
| content | TEXT | 日志内容 |
| created_at | DATETIME | 创建时间 |

**日志清理机制**：
- 日志暂时存储在数据库中。
- 当日志达到500条时，触发日志输出：所有累积日志以txt文件形式输出到日志文件夹，然后已输出的日志从数据库中清除。
- txt日志文件按日期命名。